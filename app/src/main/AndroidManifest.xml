<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MADLabexam02RecipeApp">
        <activity
            android:name=".Kkrlk"
            android:exported="false" />
        <activity
            android:name=".RecipeDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".FavoriteActivity"
            android:exported="false" />
        <activity
            android:name=".CategoryActivity"
            android:exported="false" /> <!-- Home -->
        <activity
            android:name=".HomeActivity"
            android:exported="true" /> <!-- Onboarding Screens -->
        <activity
            android:name=".Onboard3Activity"
            android:exported="true" />
        <activity
            android:name=".Onboard2Activity"
            android:exported="true" />
        <activity
            android:name=".Onboard1Activity"
            android:exported="true" /> <!-- Signup -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true" /> <!-- Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" /> <!-- Launcher (first activity to run) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>